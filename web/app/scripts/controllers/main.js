'use strict';

/**
 * @ngdoc function
 * @name csfidelisApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the csfidelisApp
 */
angular.module('csfidelisApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
